# Copy to .env and fill in from the app repo's .env.local
# (EXPO_PUBLIC_SUPABASE_URL / EXPO_PUBLIC_SUPABASE_ANON_KEY).
# These are public client keys — safe to expose in a frontend build.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

node_modules
dist
dist-ssr
*.local
.env
.env.local
.DS_Store
.vscode/*
!.vscode/extensions.json

# Local deploy artifacts
*.zip
.ftp-deploy-sync-state.json

# TypeScript incremental build cache — machine-specific, and it conflicted on
# every PR that touched a component. Build output, not source.
*.tsbuildinfo
